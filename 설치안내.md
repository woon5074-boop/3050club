# 3050파크골프클럽 홈페이지 — 설치 안내

## 폴더 구성
- `index.html` · `app.js` : 홈페이지 본체 (모바일 우선, 5개 탭 + 운영진 관리)
- `firebase-config.js` : Firebase 설정값 입력 파일 (비어 있으면 체험 모드)
- `firestore.rules` : 데이터 보안 규칙 / `firebase.json` : 호스팅 설정
- `assets/` : 로고, 사진, 공유용 이미지

## 1. 바로 확인 (체험 모드)
`index.html`을 더블클릭하면 샘플 데이터로 전체 기능을 체험할 수 있습니다. 상단의 "운영진으로 체험"을 누르면 운영진 화면까지 확인됩니다. 체험 데이터는 해당 브라우저에만 저장됩니다.

## 2. 실제 운영 전환 (Firebase, 무료 Spark 요금제로 가능)
1. https://console.firebase.google.com 에서 프로젝트 생성 (예: club3050)
2. 빌드 > Authentication > 시작하기 > 로그인 방법에서 **익명** 사용 설정
3. 빌드 > Firestore Database > 데이터베이스 만들기 (위치: asia-northeast3 서울, 프로덕션 모드)
4. 프로젝트 설정 > 내 앱 > 웹 앱 추가 > 표시되는 `firebaseConfig` 값을 `firebase-config.js`에 붙여 넣기
5. PC에 Node.js 설치 후 이 폴더에서 실행
   ```
   npm install -g firebase-tools
   firebase login
   firebase use --add        (위에서 만든 프로젝트 선택)
   firebase deploy
   ```
   규칙(firestore.rules)과 홈페이지가 함께 배포됩니다.

## 3. 첫 운영진 계정 만들기 (최초 1회)
1. 배포된 홈페이지에서 본인 이름·휴대폰으로 **가입 신청**
2. Firebase 콘솔 > Firestore > `members` 컬렉션 > 본인 문서(이름_뒷4자리)에서
   `status` 를 `approved`, `admin` 을 `true`, `role` 을 `회장`(또는 총무)로 수정
3. 홈페이지에서 이름 + 휴대폰 뒷자리 4자리로 로그인 → 우측 상단 톱니바퀴가 운영진 관리
   이후 회원 승인·권한 부여는 모두 홈페이지 안에서 처리합니다.

## 4. 도메인 연결
Firebase 콘솔 > Hosting > 커스텀 도메인 추가 > `www.3050club.co.kr` 입력 → 안내되는 A/TXT 레코드를 도메인 등록업체 DNS에 입력. `3050club.kr`, `3050club.com`도 같은 방법으로 추가하면 www.3050club.co.kr 로 자동 연결(리디렉션)할 수 있습니다.

## 운영 참고
- 로그인은 편의를 우선한 방식(이름 + 휴대폰 뒷자리)입니다. 휴대폰 전체 번호는 운영진만 볼 수 있도록 별도 보관됩니다.
- 사진은 업로드 시 자동 축소되어 Firestore에 저장되므로 유료 Storage가 필요 없습니다.
- 회비 방식: 가입비(가입 시 1회) + 월례회 참가비(참석 신청자만 납부). 금액은 운영진 관리 > 클럽 설정 > 회비 금액 설정, 월례회별 참가비는 월례회 등록·수정 화면에서 변경
- 직책 목록: 운영진 관리 > 클럽 설정 > 직책 설정 (기본: 단장·사무장·총무·경기위원·고문·정회원·신입)
- 회비 계좌(국민은행 0304050777, 예금주 3050파크골프)와 밴드 주소는 `app.js` 맨 위 `CLUB` 항목에서 수정합니다.

## 월례회 운영 순서 (운영진)
1. 월례회 등록 → 전체 회원에게 알림
2. 참석 신청 접수 · 참가비 입금 확인 (전화·현장 접수와 외부 참가자는 "참가자 직접 등록")
3. 조 자동 편성(초안) → 이름을 눌러 조 이동·조장 지정 → "조 편성 발표" (회원 알림, 밴드·카톡용 "조편성 복사")
4. 조 편성·스코어: 각 조 조장이 홀별 스코어카드 입력 후 제출 (운영진은 모든 조 입력·수정 가능, 합계만 넣는 간편 입력도 가능)
5. "결과 확정" → 랭킹 반영(동타는 후반 9홀 → 마지막 6홀 → 3홀 순), 홀인원 자동 기록, 전체 알림

## 알림
로그인한 회원은 화면 위 종 아이콘에서 알림을 확인합니다(새 월례회, 조 편성 발표, 결과 발표, 공지, 가입 승인, 입금 확인 / 운영진: 가입 신청, 스코어 제출). 홈페이지를 열어 둔 동안에는 기기 알림 팝업도 받을 수 있습니다. 앱을 닫아도 오는 푸시 알림은 Firebase Functions 배포가 필요해 아직 포함하지 않았습니다.
